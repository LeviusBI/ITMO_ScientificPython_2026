# ViT Image Reconstruction



## Usage (better to use uv pip install)

1.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Run reconstruction:**
    ```bash
    python restore_image.py
    ```

3.  **Output:**
    The script will generate `restored_image.jpg` in the current working directory.

